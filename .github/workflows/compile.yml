name: Webserv compilation and Siege test

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build_and_test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y g++ make

      - name: Build
        run: |
          make

      - name: Install Siege
        run: |
          sudo apt-get install -y siege

      - name: Run Server
        run: |
          nohup bin/webserv > server_log.txt 2>&1 &

      - name: Wait for Server to Start
        run: |
          sleep 5  # Wait for the server to start, adjust the duration if needed

      - name: Run Siege Test
        run: |
          siege -c 10 -t 2M http://localhost:8000 > siege_output.txt

      - name: Stop Server
        run: |
          pkill webserv

      - name: Save Siege Output as Artifact
        uses: actions/upload-artifact@v2
        with:
          name: siege_output
          path: siege_output.txt
